import React from "react";

type props = {
  arrayOfHours: { id: number; time: string }[];
  setTaskEndTime: React.Dispatch<React.SetStateAction<string>>;
  taskStartTime: string;
};

const ListOfHoursENd = () => {
  return <div></div>;
};

export default ListOfHoursENd;
